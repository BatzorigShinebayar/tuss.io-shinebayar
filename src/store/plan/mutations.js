export default {
  companyPlan(state, companyPlan) {
    state.companyPlan = companyPlan;
  },

  task(state, task) {
    state.task = task;
  },

  taskType(state, taskType) {
    state.taskType = taskType;
  },

  taskStageState(state, taskStageState) {
    state.taskStageState = taskStageState;
  },

  taskStageGroup(state, taskStageGroup) {
    state.taskStageGroup = taskStageGroup;
  },

  taskYearId(state, taskYearId) {
    state.taskYearId = taskYearId;
  },

  taskFilter(state, taskFilter) {
    state.taskFilter = taskFilter;
  },

  taskSort(state, taskSort) {
    state.taskSort = taskSort;
  },

  taskData(state, taskData) {
    state.taskData = taskData;
  },
};
